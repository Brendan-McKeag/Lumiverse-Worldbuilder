{
  "name": "lumiverse-worldbuilder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/backend.ts --outfile dist/backend.js --target bun && bun build src/frontend.ts --outfile dist/frontend.js --target browser"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "lumiverse-spindle-types": "^0.5.13",
    "typescript": "^6.0.3"
  }
}
